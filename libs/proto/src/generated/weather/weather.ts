// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v7.34.0
// source: weather/weather.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, Metadata, UntypedServiceImplementation } from "@grpc/grpc-js";
import { GrpcMethod, GrpcStreamMethod } from "@nestjs/microservices";
import { Observable } from "rxjs";

export const protobufPackage = "weather";

export interface WeatherInput {
}

export interface WeatherOutput {
}

export const WEATHER_PACKAGE_NAME = "weather";

function createBaseWeatherInput(): WeatherInput {
  return {};
}

export const WeatherInput: MessageFns<WeatherInput> = {
  encode(_: WeatherInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WeatherInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWeatherInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

function createBaseWeatherOutput(): WeatherOutput {
  return {};
}

export const WeatherOutput: MessageFns<WeatherOutput> = {
  encode(_: WeatherOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WeatherOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWeatherOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
};

export interface WeatherClient {
  weather(request: WeatherInput, metadata?: Metadata): Observable<WeatherOutput>;
}

export interface WeatherController {
  weather(
    request: WeatherInput,
    metadata?: Metadata,
  ): Promise<WeatherOutput> | Observable<WeatherOutput> | WeatherOutput;
}

export function WeatherControllerMethods() {
  return function (constructor: Function) {
    const grpcMethods: string[] = ["weather"];
    for (const method of grpcMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcMethod("Weather", method)(constructor.prototype[method], method, descriptor);
    }
    const grpcStreamMethods: string[] = [];
    for (const method of grpcStreamMethods) {
      const descriptor: any = Reflect.getOwnPropertyDescriptor(constructor.prototype, method);
      GrpcStreamMethod("Weather", method)(constructor.prototype[method], method, descriptor);
    }
  };
}

export const WEATHER_SERVICE_NAME = "Weather";

export type WeatherService = typeof WeatherService;
export const WeatherService = {
  weather: {
    path: "/weather.Weather/weather",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: WeatherInput): Buffer => Buffer.from(WeatherInput.encode(value).finish()),
    requestDeserialize: (value: Buffer): WeatherInput => WeatherInput.decode(value),
    responseSerialize: (value: WeatherOutput): Buffer => Buffer.from(WeatherOutput.encode(value).finish()),
    responseDeserialize: (value: Buffer): WeatherOutput => WeatherOutput.decode(value),
  },
} as const;

export interface WeatherServer extends UntypedServiceImplementation {
  weather: handleUnaryCall<WeatherInput, WeatherOutput>;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
}
