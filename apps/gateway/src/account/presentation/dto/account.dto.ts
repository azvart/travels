import { ObjectType, Field, ID } from '@nestjs/graphql';import { User } from './user.dto';import { IAccount } from 'libs/interfaces';@ObjectType()export class Account implements IAccount {  @Field(() => ID)  public id!: string;  @Field(() => String)  public email!: string;  @Field(() => String)  public password!: string;  @Field(() => String)  public registrationType!: string;  @Field(() => User, { nullable: true })  public user!: User;}