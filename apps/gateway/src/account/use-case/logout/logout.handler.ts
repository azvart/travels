import { Injectable } from '@nestjs/common';import { AccountGrpcService } from '@app/grpc-api-clients';import { firstValueFrom } from 'rxjs';@Injectable()export class LogoutHandler {  public constructor(    private readonly accountGrpcService: AccountGrpcService,  ){}  public async run(ctx){    return firstValueFrom(      this.accountGrpcService.service.logout({        userId: ctx.user.userId,        accountId: ctx.user.accountId,      })    )  }}